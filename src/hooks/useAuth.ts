/**
 * Re-export for backward compatibility
 * @deprecated Use import from "@/hooks/auth" or "@/hooks" instead
 */
export { useAuth, useRequireAuth } from "./auth";
export type { AuthStatus, AuthContextValue } from "./auth";
